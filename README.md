# 📰 Fake News Detection App

This project uses Machine Learning (Logistic Regression / Naive Bayes / etc.) to detect whether a given news text is **real or fake**.

## 🚀 How to Run Locally
1. Clone this repository
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
